typedef int Clé;

struct nœud{
	Clé clé;
	bool color
	struct nœud *p;
	struct nœud *gauche;
	struct nœud *droite;
};
/*
class Nœud{
private:
	Clé clé;
	struct nœud *p;
	struct nœud *gauche;
	struct nœud *droite;
public:
	Nœud(int c);
};
*/

typedef struct nœud ÉLÉMENT;
typedef ÉLÉMENT* Nœud;
